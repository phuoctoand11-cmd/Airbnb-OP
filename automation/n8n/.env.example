# Copy this file to .env. Never commit the real .env file.
# Generate a long random value locally, for example with:
#   openssl rand -hex 32
N8N_ENCRYPTION_KEY=replace-with-a-random-64-character-hex-value
N8N_PORT=5678
